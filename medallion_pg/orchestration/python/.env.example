# SQL Server ODBC connection string
# Example:
# MSSQL_CONN_STR=DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost;DATABASE=YourSourceDb;UID=sa;PWD=YourPassword;TrustServerCertificate=yes;
MSSQL_CONN_STR=

# PostgreSQL connection string
# Example:
# PG_CONN_STR=host=localhost port=5432 dbname=casino_dw user=postgres password=postgres
PG_CONN_STR=

# Bronze sliding reread window (days)
BRONZE_LOOKBACK_DAYS=3

# SMTP email notifications (Prefect run status)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_FROM_EMAIL=
SMTP_TO_EMAILS=

# Daily visit-sessions report recipients (export_visit_sessions.py)
# Active during testing rollout — keep ONLY data@tourinvestgroup.com until validated.
REPORT_TO_EMAILS=data@tourinvestgroup.com

# Team distribution list — DO NOT enable until report is validated.
# When ready, replace REPORT_TO_EMAILS with a comma-separated list including these:
#   artem.vorotilov@cibatumi.com,
#   nana.khachirova@cibatumi.com,
#   mirian.tavdgiridze@cibatumi.com,
#   tamar.vashadze@cibatumi.com,
#   natela.luashvili@cibatumi.com